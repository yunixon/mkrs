<div class="container">
<div id="item-container" class="transitions-enabled infinite-scroll clearfix">
<% @listings.each do |listing| %>
<div class="box col1">
  <div class="item-small">
    <div class="item-info">
      <%= link_to '', listing, :class => 'item-link' %>
      <p class="price"><%= listing.price %> €</p>
      <div class="item-info-bottom">
        <p class="item-name"><%= listing.name %></p>
        <p class="item-author"><a href="/<%= listing.user.name %>"><%= listing.user.name %>
        </a></p>
      </div>
    </div>

 <%= image_tag listing.photos.first.image.small.url %> 
  </div>
</div>
<% end %>
</div>








<%= link_to 'New Listing', new_listing_path %>
<%= link_to 'Redaguoti', new_listing_path %>
</div>

